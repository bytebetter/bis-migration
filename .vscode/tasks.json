{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "migrate: patient_info → Postgres (js-migrate)",
      "type": "shell",
      "command": "node",
      "args": [
        "migrate-from-mssql.mjs",
        "--config",
        "config.local.json"
      ],
      "options": { "cwd": "${workspaceFolder}/patient-info/js-migrate" },
      "presentation": { "reveal": "always", "panel": "shared", "focus": true }
    }
  ]
}
